{% extends 'base.html.twig' %}

{% block title %}Edit Products{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen bg-gray-200">
    <div class="w-full max-w-2xl bg-white p-8 rounded-xl shadow-lg">
        <h1 class="text-4xl font-bold mb-6  text-gray-800 text-center">Edit Product</h1>

        <div class="bg-gray-50 p-6 rounded-lg shadow-md">
            {{ include('products/_form.html.twig', {'button_label': 'Update'}) }}
        </div>

        <div class="mt-6 flex justify-center space-x-8 mb-8">
            <a href="{{ path('app_products_index') }}" 
               class="px-4 py-2 bg-gray-500 text-white rounded-lg shadow hover:bg-gray-600">
                ⬅ Back to list
            </a>

            {{ include('products/_delete_form.html.twig') }}
        </div>
    </div>
</div>
{% endblock %}
