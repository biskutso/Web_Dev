{% extends 'base.html.twig' %}

{% block title %}New Products{% endblock %}

{% block body %}
<div class="flex items-center justify-center min-h-screen bg-gray-200">
    <div class="w-full max-w-2xl bg-white p-8 rounded-xl shadow-lg">
        <h1 class="text-4xl font-bold mb-6  text-gray-800 text-center">Add New Product</h1>

        <div class="bg-gray-50 p-6 rounded-lg shadow-md">
            {{ include('products/_form.html.twig') }}
        </div>

        <!-- Action Buttons -->
        <div class="mt-6 mb-8 flex justify-center">
            <a href="{{ path('app_products_index') }}" 
               class="px-5 py-2 bg-gray-500 text-white rounded-lg shadow hover:bg-gray-600">
                ⬅ Back to list
            </a>
        </div>
    </div>
</div>
{% endblock %}
