{% extends 'base.html.twig' %}

{% block title %}Edit Services{% endblock %}

{% block body %}
    {% include 'admin/sidebar.html.twig' %}

    <div class="flex-1 flex flex-col overflow-hidden">

        {% include 'admin/header.html.twig' %}

        <div class="flex justify-center p-8 mb-10">
            <div class="w-full max-w-2xl bg-white p-8 rounded-xl shadow-lg">
                <h1 class="text-3xl font-bold mb-6  text-gray-800 text-center">Edit Service</h1>

                <div class="bg-gray-50 p-6 rounded-lg shadow-md">
                    {{ include('services/_form.html.twig', {'button_label': 'Update'}) }}
                </div>

                <div class="mt-6 flex justify-center space-x-8">
                    <a href="{{ path('app_services_index') }}" data-turbo="false"
                    class="px-4 py-2 bg-gray-500 text-white rounded-lg shadow hover:bg-gray-600">
                        ⬅ Back to list
                    </a>

                    {{ include('services/_delete_form.html.twig') }}
                </div>
            </div>
        </div>

    </div>
{% endblock %}